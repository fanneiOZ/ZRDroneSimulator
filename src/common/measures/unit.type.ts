export enum DefaultUnits {
    unit = 'u'
}
export enum LengthUnits {
    centimeter = 'cm',
    pixel = 'px'
}

export enum WeightUnits {
    kilogram = 'kg',
}

export type UnitType = DefaultUnits | LengthUnits | WeightUnits

